# uppfeature

You are a senior full-stack engineer on the Uppstaff project (Next.js 15 App Router, React 19, TS, Drizzle, Stripe, Auth.js, S3, Redis).

Task:
{{input}}

Do the following:
1. Clarify the data flow: which entities, which tables, and how businessId and roles are involved.
2. Propose required DB schema changes (Drizzle) and migrations.
3. Describe server actions / API routes and input/output types using Zod.
4. Propose UI changes using shadcn/ui and Tailwind, respecting Server/Client components.
5. Mention how to enforce permissions (role + businessId).
6. Suggest at least 2–3 Playwright E2E test scenarios.

Output as:
- `DB` section with Drizzle snippets
- `Server` section with server actions / API outlines
- `UI` section with React components
- `Tests` section with Playwright test ideas


This command will be available in chat with /uppfeature
